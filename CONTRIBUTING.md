# 🤝 دليل المساهمة في مشروع رفيق القرآن

<div dir="rtl">

شكراً لاهتمامك بالمساهمة في تطوير **رفيق القرآن**! هذا الدليل يشرح كيفية المساهمة بفعالية.

## 🚀 البدء السريع

```bash
# 1. استنساخ المشروع
git clone https://github.com/USERNAME/rafiq-alquran.git
cd rafiq-alquran

# 2. تشغيل خادم محلي (اختياري لكن موصى به)
python3 -m http.server 8000
# ثم افتح http://localhost:8000

# 3. إنشاء فرع جديد للتعديلات
git checkout -b feature/اسم-الميزة
```

## 📋 قواعد المساهمة

### 1. هيكل الكود
- **HTML**: حافظ على ملف `index.html` نظيفاً وبدون أنماط أو سكربتات مدمجة.
- **CSS**: كل الأنماط تذهب إلى `css/styles.css` — استخدم متغيرات CSS الموجودة.
- **JavaScript**:
  - `js/app-data.js` — للبيانات ومنطق الأعمال وإدارة الحالة.
  - `js/app-ui.js` — لواجهة المستخدم ودوال العرض فقط.
- **الأصول**: ضع أي صور أو خطوط جديدة في مجلد `assets/`.

### 2. اصطلاحات التسمية
- **الدوال**: `camelCase` مثل `renderDashboard()`.
- **المتغيرات**: `camelCase` مثل `app_view`.
- **الثوابت الكبرى**: `UPPER_SNAKE_CASE` مثل `SURAH_META`.
- **الملفات**: `kebab-case` للأسماء متعددة الكلمات.

### 3. التعليقات
- التعليقات بالعربية 🇸🇦 (للحفاظ على توحد المشروع).
- علّق كل دالة جديدة بشرح مختصر لما تفعله.
- استخدم فاصل `// ================== NAME ==================` للأقسام الكبرى.

### 4. الالتزامات (Commits)
- استخدم رسائل واضحة بالعربية أو الإنجليزية.
- الصيغة المفضلة: `نوع: وصف مختصر`
- أمثلة:
  - `ميزة: إضافة وضع التسميع الصوتي`
  - `إصلاح: حل مشكلة عرض التقويم في الوضع الليلي`
  - `تحسين: تسريع تحميل بيانات السور`

### 5. الاختبار قبل الإرسال
- ✅ تأكد أن التطبيق يعمل في Chrome و Firefox.
- ✅ اختبر الوضع الفاتح والداكن.
- ✅ اختبر على شاشة الجوال (Responsive).
- ✅ تأكد أن لا توجد أخطاء في Console.
- ✅ اختبر أن البيانات تُحفظ في `localStorage` ولا تُفقد عند التحديث.

## 🎯 مجالات المساهمة المرحب بها

### أولوية عالية 🔴
- إصلاح الأخطاء (Bugs).
- تحسين تجربة المستخدم على الجوال.
- إضافة دعم تصدير/استيراد البيانات (JSON).

### أولوية متوسطة 🟡
- دعم التلاوات الصوتية (مع التأكد من الأذونات الشرعية).
- دعم لغات إضافية (الإنجليزية).
- تحسين أداء التحميل الأولي.

### أولوية منخفضة 🟢
- تحسين التصميم البصري.
- إضافة شارات إنجازات جديدة.
- تحسين التقارير برسوم بيانية.

## 🔄 سير العمل (Workflow)

1. **افتح Issue** أولاً لمناقشة الميزة الكبيرة قبل البدء.
2. **اعمل Fork** ثم أنشئ فرع جديد.
3. **اكتب الكود** مع اتباع القواعد أعلاه.
4. **اختبر محلياً** بشكل كامل.
5. **افتح Pull Request** مع وصف واضح للتعديلات.
6. **انتظر المراجعة** — سنرد خلال 3-5 أيام.

## ❌ ما لا نريد

- ❌ إضافة مكتبات Frameworks ثقيلة (المشروع يعتمد على Vanilla JS).
- ❌ تغيير بنية الملفات دون مناقشة مسبقة.
- ❌ حذف أو تعديل بيانات السور القرآنية بدون مرجع موثوق.
- ❌ إضافة ميزات تتعارض مع الطابع الإسلامي للتطبيق.

## 📞 للأسئلة

- افتح [Issue](../../issues) للأسئلة العامة.
- للأمور الخاصة، راسل عبر GitHub.

---

> ﴿ وَقُل رَّبِّ زِدْنِي عِلْمًا ﴾ — طه: 114

</div>
